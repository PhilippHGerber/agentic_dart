# agentic_dart monorepo — Dart workspace root.
name: agentic_dart
publish_to: none

environment:
  sdk: ">=3.9.0 <4.0.0"

workspace:
  - packages/pubdev_context

dev_dependencies:
  test: ^1.31.0
  very_good_analysis: ^10.2.0
