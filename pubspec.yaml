# agentic_dart monorepo workspace root.
name: agentic_dart
publish_to: none

environment:
  sdk: ">=3.9.0 <4.0.0"

workspace:
  - packages/dart_pubdev_mcp

dev_dependencies:
  test: ^1.31.0
  very_good_analysis: ^11.0.0
