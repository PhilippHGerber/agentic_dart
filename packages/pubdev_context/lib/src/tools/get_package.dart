/// Handler for the `get_package` MCP tool.
///
/// Returns full `PackageDetail` for one package, optionally at a pinned version.
/// Fetches metadata, score, and metrics in a single logical call.
///
/// See `issues/pub-dev-mcp/06-get-package-tool.md`.
library;

// TODO(issues/pub-dev-mcp/06): implement GetPackageHandler
