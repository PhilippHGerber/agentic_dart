/// Resource handlers for the pub://meta/ namespace.
///
/// Serves two static resources:
///   - pub://meta/scoring     — pub.dev 160-point scoring system explanation
///   - pub://meta/sdk-versions — current stable Dart and Flutter SDK versions
///
/// Both are cached with a 24-hour TTL.
/// See issue #10.
library;

// TODO(#10): implement meta resource handlers
