# pubdev_context example

A minimal demonstration of `pubdev_context` searching for packages via the MCP protocol.

## Running the example

```bash
# Activate pubdev_context globally
dart pub global activate -s path ..

# Start pubdev_context and query it via an MCP client
pubdev_context
```

See the parent [README](../README.md) for full MCP client configuration.
