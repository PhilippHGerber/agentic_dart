# Integration tests live in test/integration/ and require a live network.
# They are tagged `integration` and excluded from the default `dart test` run.
# To run them: dart test test/integration/
tags:
  integration:
    timeout: 60s

paths:
  - test/unit

exclude_paths:
  - test/integration
